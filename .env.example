# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Admin credentials (stored in Supabase)
# Create an admin user in your database with these credentials

# Contact form email delivery (used by /api/contact in dev + serverless)
# Provide SMTP credentials from your email provider.
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_USER=ventasbbca@serviningenieria.com.ar
SMTP_PASS=your_smtp_password

# Optional overrides
CONTACT_TO=ventasbbca@serviningenieria.com.ar
CONTACT_FROM=ventasbbca@serviningenieria.com.ar
