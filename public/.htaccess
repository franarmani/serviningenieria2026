# SERVIN INGENIERÍA - Apache Configuration for React SPA

# Enable Rewrite Engine
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Don't rewrite files or directories that exist
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]
  
  # Allow PHP files directly (contact.php)
  RewriteCond %{REQUEST_FILENAME} \.php$
  RewriteRule ^ - [L]
  
  # Don't rewrite API calls - Allow them to pass through
  RewriteCond %{REQUEST_URI} ^/api/
  RewriteRule ^ - [L]
  
  # Rewrite everything else to index.html (React Router)
  RewriteRule ^ index.html [L]
</IfModule>

# Security Headers
<IfModule mod_headers.c>
  # Prevent clickjacking
  Header set X-Frame-Options "DENY"
  
  # Prevent MIME sniffing
  Header set X-Content-Type-Options "nosniff"
  
  # XSS Protection
  Header set X-XSS-Protection "1; mode=block"
  
  # Referrer Policy
  Header set Referrer-Policy "same-origin"
  
  # Cache control for static assets
  <FilesMatch "\.(jpg|jpeg|png|gif|webp|svg|woff|woff2|ttf|css|js)$">
    Header set Cache-Control "max-age=31536000, public"
  </FilesMatch>
</IfModule>

# Compression
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# Default charset
AddDefaultCharset UTF-8

# Prevent directory listing
Options -Indexes

# Custom error pages (optional)
# ErrorDocument 404 /index.html
# ErrorDocument 500 /index.html
